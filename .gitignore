node_modules
dist
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Test environment variables (real creds; e2e/.env.test.example is checked in)
/e2e/.env.test

# Scaffolds created for dev-testing inside this repo (should be scaffolded
# outside this directory to avoid npm nested-install collisions).
/my-test-app
/test-app

# Playwright outputs
playwright-report
test-results

# Template artifacts (when developing locally inside template/)
templates/*/template/node_modules
templates/*/template/dist
templates/*/template/package-lock.json
